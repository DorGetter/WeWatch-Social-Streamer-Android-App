<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wewatchapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WeWatchApp">
        <activity android:name=".utilitiesPack.VideoplayActivity"></activity>
        <activity android:name=".utilitiesPack.MovieDetailNewActivity" />
        <activity android:name=".userPack.Vod" />
        <activity android:name=".managerPack.ProfileManager" />
        <activity android:name=".userPack.ProfileUser" />
        <activity android:name=".Home.ForgotPassword" />
        <activity android:name=".managerPack.UploadMovie" />
        <activity android:name=".Home.RegisterUser" />
        <activity android:name=".Home.RegisterManager" />
        <activity android:name=".utilitiesPack.UploadThumnailActivity" />
        <activity android:name=".Home.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>